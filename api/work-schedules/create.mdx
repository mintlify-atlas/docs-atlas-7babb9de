---
title: "Create Work Schedule"
api: "POST /work_schedules"
description: "Create a new work schedule for a professional"
---

## Authentication

Requires a valid JWT token in the request header.

**Admin privileges required** - Only administrators can create work schedules.

## Request Body

<ParamField body="professionalID" type="integer" required>
  ID of the professional for whom the schedule is being created
  
  Must reference an existing professional in the system.
</ParamField>

<ParamField body="dayOfWeek" type="integer" required>
  Day of the week for this schedule
  
  - `0` = Sunday (Domingo)
  - `1` = Monday (Lunes)
  - `2` = Tuesday (Martes)
  - `3` = Wednesday (Miércoles)
  - `4` = Thursday (Jueves)
  - `5` = Friday (Viernes)
  - `6` = Saturday (Sábado)
  
  **Constraint**: Each professional can have only one schedule block per day.
</ParamField>

<ParamField body="startTime" type="string" required>
  Start time of the work schedule in HH:MM format
  
  **Format**: `HH:MM` (24-hour format)
  
  **Examples**:
  - `"09:00"` - 9:00 AM
  - `"14:30"` - 2:30 PM
  - `"08:00"` - 8:00 AM
  
  **Validation**:
  - Hours: `00-23`
  - Minutes: `00-59`
  - Must match pattern: `/^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$/`
</ParamField>

<ParamField body="endTime" type="string" required>
  End time of the work schedule in HH:MM format
  
  **Format**: `HH:MM` (24-hour format)
  
  **Examples**:
  - `"17:30"` - 5:30 PM
  - `"13:00"` - 1:00 PM
  - `"20:00"` - 8:00 PM
  
  **Validation**:
  - Hours: `00-23`
  - Minutes: `00-59`
  - Must match pattern: `/^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$/`
  - **Must be later than `startTime`**
</ParamField>

## Response

<ResponseField name="schedule" type="object">
  The created work schedule object
  
  <Expandable title="properties">
    <ResponseField name="id" type="integer">
      Unique identifier for the created schedule
    </ResponseField>
    
    <ResponseField name="professionalID" type="integer">
      ID of the professional
    </ResponseField>
    
    <ResponseField name="dayOfWeek" type="integer">
      Day of the week (0-6)
    </ResponseField>
    
    <ResponseField name="startTime" type="string">
      Start time in HH:MM format
    </ResponseField>
    
    <ResponseField name="endTime" type="string">
      End time in HH:MM format
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST "https://api.clinicavitalis.com/work_schedules" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "professionalID": 5,
    "dayOfWeek": 1,
    "startTime": "09:00",
    "endTime": "17:30"
  }'
```

```javascript JavaScript
const response = await fetch(
  'https://api.clinicavitalis.com/work_schedules',
  {
    method: 'POST',
    headers: {
      'Authorization': 'Bearer YOUR_JWT_TOKEN',
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      professionalID: 5,
      dayOfWeek: 1,
      startTime: '09:00',
      endTime: '17:30'
    })
  }
);

const data = await response.json();
```
</RequestExample>

<ResponseExample>
```json 201 - Created
{
  "schedule": {
    "id": 15,
    "professionalID": 5,
    "dayOfWeek": 1,
    "startTime": "09:00",
    "endTime": "17:30"
  }
}
```

```json 400 - Validation Error (Invalid Time Format)
{
  "errors": [
    {
      "msg": "El formato de hora debe ser HH:mm (Ej: 09:30)",
      "param": "startTime"
    }
  ]
}
```

```json 400 - Validation Error (Invalid Time Range)
{
  "errors": [
    {
      "msg": "La hora de finalización debe ser posterior a la hora de inicio",
      "param": "endTime"
    }
  ]
}
```

```json 400 - Validation Error (Invalid Day)
{
  "errors": [
    {
      "msg": "El día debe ser un número entre 0 (Domingo) y 6 (Sábado)",
      "param": "dayOfWeek"
    }
  ]
}
```

```json 400 - Professional Not Found
{
  "errors": [
    {
      "msg": "El profesional con ID 99 no existe",
      "param": "professionalID"
    }
  ]
}
```

```json 409 - Duplicate Schedule
{
  "msg": "El profesional ya tiene horarios configurados para este día."
}
```

```json 500 - Server Error
{
  "msg": "Error del servidor"
}
```
</ResponseExample>

## Professional Schedule Constraints

### Unique Day Constraint

Each professional can have **only one schedule block per day** of the week. This constraint is enforced at the database level through a unique index on `(professionalID, dayOfWeek)`.

**Example Scenarios**:

✅ **Valid**: Dr. García works Monday 09:00-17:30

❌ **Invalid**: Dr. García works Monday 09:00-13:00 AND Monday 15:00-19:00

If you need to define split schedules (morning and afternoon), you must use a single continuous block or handle breaks in the appointment logic.

### Time Format Validation

Both `startTime` and `endTime` must match the HH:MM format:

- **Valid**: `"09:00"`, `"14:30"`, `"08:15"`, `"23:59"`
- **Invalid**: `"9:00"` (missing leading zero), `"09:00:00"` (includes seconds), `"25:00"` (invalid hour)

### Time Range Validation

The `endTime` must be **strictly greater** than `startTime`:

- ✅ `startTime: "09:00"`, `endTime: "17:30"`
- ❌ `startTime: "09:00"`, `endTime: "09:00"`
- ❌ `startTime: "17:00"`, `endTime: "09:00"`

## Notes

- Work schedules are used to validate appointment availability
- Appointments can only be created during the professional's defined work hours
- The system checks that appointment times fall within the `startTime` and `endTime` range
- Changes to work schedules may affect existing appointments