---
title: 'Create Social Work'
api: 'POST /socials_works'
---

Creates a new social work (health insurance provider) in the system.

## Authentication

Requires a valid JWT token in the Authorization header and admin privileges.

```
Authorization: Bearer {token}
```

<Warning>
  This endpoint requires **admin role**. Non-admin users will receive a 403 Forbidden error.
</Warning>

## Request Body

<ParamField body="name" type="string" required>
  Name of the social work (health insurance provider).
  
  **Validation:**
  - Cannot be empty
  - Must be unique in the system
  
  Example: `"OSDE"`
</ParamField>

<ParamField body="address" type="string" required>
  Physical address of the social work office.
  
  **Validation:**
  - Cannot be empty
  
  Example: `"Av. Leandro N. Alem 1067, CABA"`
</ParamField>

<ParamField body="phone" type="string" required>
  Contact phone number for the social work.
  
  **Validation:**
  - Cannot be empty
  - Must be between 10 and 20 characters
  - Can only contain numbers, spaces, hyphens, and plus signs
  - Pattern: `/^[0-9\s\-\+]*$/`
  
  Examples:
  - `"+54 11 4310-5555"`
  - `"0800-444-1200"`
  - `"11 5777 7777"`
</ParamField>

<ParamField body="webpage" type="string" required>
  Website URL of the social work.
  
  **Validation:**
  - Cannot be empty
  
  Example: `"https://www.osde.com.ar"`
</ParamField>

## Response

<ResponseField name="socialWork" type="object">
  The newly created social work object
  
  <Expandable title="Properties">
    <ResponseField name="id" type="number">
      Auto-generated unique identifier
    </ResponseField>
    
    <ResponseField name="name" type="string">
      Name of the social work
    </ResponseField>
    
    <ResponseField name="state" type="string">
      Default state set to `"Activa"` upon creation
    </ResponseField>
    
    <ResponseField name="address" type="string">
      Physical address of the social work
    </ResponseField>
    
    <ResponseField name="phone" type="string">
      Contact phone number
    </ResponseField>
    
    <ResponseField name="webpage" type="string">
      Website URL
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseExample>
```json 201 - Created
{
  "socialWork": {
    "id": 4,
    "name": "OSDE",
    "state": "Activa",
    "address": "Av. Leandro N. Alem 1067, CABA",
    "phone": "+54 11 4310-5555",
    "webpage": "https://www.osde.com.ar"
  }
}
```

```json 400 - Validation Error (Duplicate Name)
{
  "errors": [
    {
      "msg": "El nombre de la obra social ya existe",
      "param": "name",
      "location": "body"
    }
  ]
}
```

```json 400 - Validation Error (Invalid Phone)
{
  "errors": [
    {
      "msg": "El teléfono debe tener entre 10 y 20 caracteres",
      "param": "phone",
      "location": "body"
    }
  ]
}
```

```json 400 - Validation Error (Phone Format)
{
  "errors": [
    {
      "msg": "El teléfono solo puede contener números, guiones o espacios",
      "param": "phone",
      "location": "body"
    }
  ]
}
```

```json 400 - Validation Error (Missing Fields)
{
  "errors": [
    {
      "msg": "El nombre es obligatorio",
      "param": "name",
      "location": "body"
    },
    {
      "msg": "La dirección es obligatoria",
      "param": "address",
      "location": "body"
    }
  ]
}
```

```json 403 - Forbidden (Not Admin)
{
  "msg": "Acceso denegado. Se requieren permisos de administrador"
}
```

```json 401 - Unauthorized
{
  "msg": "Token no válido"
}
```

```json 500 - Server Error
{
  "msg": "Error del servidor"
}
```
</ResponseExample>

## Code Examples

<CodeGroup>
```javascript JavaScript
const token = 'your-admin-jwt-token';

const newSocialWork = {
  name: 'OSDE',
  address: 'Av. Leandro N. Alem 1067, CABA',
  phone: '+54 11 4310-5555',
  webpage: 'https://www.osde.com.ar'
};

const response = await fetch('https://api.clinicavitalis.com/socials_works', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${token}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify(newSocialWork)
});

const data = await response.json();
console.log(data.socialWork);
```

```python Python
import requests

token = 'your-admin-jwt-token'
url = 'https://api.clinicavitalis.com/socials_works'

headers = {
    'Authorization': f'Bearer {token}',
    'Content-Type': 'application/json'
}

payload = {
    'name': 'OSDE',
    'address': 'Av. Leandro N. Alem 1067, CABA',
    'phone': '+54 11 4310-5555',
    'webpage': 'https://www.osde.com.ar'
}

response = requests.post(url, json=payload, headers=headers)
data = response.json()
print(data['socialWork'])
```

```bash cURL
curl -X POST "https://api.clinicavitalis.com/socials_works" \
  -H "Authorization: Bearer your-admin-jwt-token" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "OSDE",
    "address": "Av. Leandro N. Alem 1067, CABA",
    "phone": "+54 11 4310-5555",
    "webpage": "https://www.osde.com.ar"
  }'
```
</CodeGroup>

## Validation Rules

### Name Validation
- Must be provided (not empty)
- Must be unique across all social works in the system
- Database will enforce uniqueness constraint

### Phone Validation
- **Length**: Between 10 and 20 characters
- **Format**: Can only contain:
  - Numbers (0-9)
  - Spaces
  - Hyphens (-)
  - Plus sign (+)
- **Valid examples**:
  - `"+54 11 4310-5555"`
  - `"0800-444-1200"`
  - `"11 5777 7777"`
  - `"+54-11-4310-5555"`
- **Invalid examples**:
  - `"123"` (too short)
  - `"(011) 4310-5555"` (parentheses not allowed)
  - `"tel:1234567890"` (letters not allowed)

### Address & Webpage
- Both fields are required and cannot be empty
- No format restrictions beyond being non-empty strings

## State Management

- New social works are automatically created with state `"Activa"` (Active)
- The state field is set by default and cannot be specified during creation
- To change the state to `"Inactiva"`, use the [Update Social Work](/api/social-works/update) endpoint

## Notes

- Only administrators can create social works
- The social work name must be unique in the system
- The state is automatically set to "Activa" upon creation
- All fields except state are required in the request body
- Validation errors will return a 400 status with detailed error messages